/**
 * The Twitter card is the same sheet as the Open Graph card. Re-exported rather
 * than duplicated, so the two can never drift into showing different counts.
 */
export { default, alt, size, contentType } from "./opengraph-image";
